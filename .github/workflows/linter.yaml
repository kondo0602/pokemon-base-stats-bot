name: Lint Code Base

on: pull_request

jobs:
  build:
    name: Lint Code Base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Npm Install
        name: npm install

      - name: Lint Code Base
        name: eslint "/**/*.js"
